<%= simple_form_for cocktail do |f| %>
  <%= f.input :name, label: "Nom du cocktail", error: 'Your cocktail needs a unique name' %>
  <%= f.input :photo %>
  <%= f.input :photo_cache, as: :hidden %>
  <%= f.button :submit, class: "btn btn-primary"%>
<% end %>

